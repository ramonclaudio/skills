---
description: Show QMD index status (Bash fallback — works when MCP is down)
disable-model-invocation: true
---

Run `qmd status`. Report collections, document counts, and pending embeddings. If `qmd` is not found, tell the user to install it.
