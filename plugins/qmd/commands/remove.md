---
description: Remove a collection from the QMD index
allowed-tools: Bash(qmd collection remove*)
argument-hint: <collection-name>
---

Run `qmd collection remove $ARGUMENTS`. Do NOT delete the repo from ~/Developer/refs/ unless the user explicitly asks — if they do, use `trash`, never `rm`.
